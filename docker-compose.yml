version: '3.9'
services:

  postgres:
    image: postgres:13.3
    environment:
      POSTGRES_DB: "demo"
      POSTGRES_USER: "root"
      POSTGRES_PASSWORD: "root"
      PGDATA: "/var/lib/postgresql/data/pgdata"
    volumes:
      - .:/var/lib/postgresql/data
    ports:
      - "5438:5432"
